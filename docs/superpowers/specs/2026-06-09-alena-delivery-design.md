# אתר משלוחים — עלינא בפיתה

**תאריך:** 2026-06-09
**בעלים:** Dvir Nifusi
**סטטוס:** טיוטה לאישור
**דומיין:** alenabepita.co.il (וורדפרס בקופסה עצמית)

---

## 1. מטרה

לבנות אתר משלוחים חדש לרשת **עלינא בפיתה** על גבי הדומיין `alenabepita.co.il`, כך ש:

- לקוחות יוכלו להזמין משלוחים ישירות מהאתר (תחליף ל-Wolt וההזמנות הטלפוניות).
- המוצרים יסונכרנו אוטומטית מ-**Be'teavon** (המערכת שכבר עובדים איתה — לא בנייה שלנו).
- מועדון הלקוחות יתחבר ישירות ל-**TOPALENA.COM** (מקור אמת יחיד).
- בעלים יוכל לעלות מבצעים, ספיישלים, פופאפים ובאנרים בעצמו דרך אדמין וורדפרס.
- בהמשך — **ValueCard יוצא לגמרי** מהמערכת (חודש אחרי שהאתר עולה).

## 2. אילוצים

| אילוץ | סיבה |
|------|------|
| חייב להיות וורדפרס | Be'teavon מסנכרן ישירות לוורדפרס; אין שליטה על זה |
| WooCommerce חובה | סטנדרט הסנכרון של בתאבון |
| סולק: **PayPlus** | חוזה כבר נחתם, יאושר 2026-06-10 |
| RTL מלא, עברית בלבד | קהל מקומי |
| מקור אמת ללקוחות: TOPALENA | החלטה אסטרטגית — מאחדים את כל המועדון |

## 3. ארכיטקטורה

```
┌─────────────────────────────────────────────┐         ┌──────────────────────────┐
│  alenabepita.co.il  —  WordPress + WC      │ ←─────→ │  TOPALENA.COM API        │
│                                              │  HTTPS  │  (Express + Prisma)      │
│  · WooCommerce — קטלוג, סל, תשלום         │  + JWT  │                          │
│  · PayPlus gateway — סליקה                  │         │  · Customer (טלפון,     │
│  · Be'teavon plugin — מסנכרן מוצרים         │         │     coin_balance,        │
│    (לא אנחנו)                                │         │     loyalty_tier)        │
│  · Alena-TOPALENA-Bridge plugin (אנחנו)    │         │  · CustomerBenefit       │
│    — חיבור למועדון, נקודות, התראות         │         │  · Order log             │
└─────────────────────────────────────────────┘         └──────────────────────────┘
```

### מודל המועדון ב-TOPALENA (כבר קיים)

מצאנו בסכמת Prisma הקיימת (`apps/api/prisma/schema.prisma`):

```prisma
model Customer {
  phone               String
  name                String?
  visit_count         Int?      @default(0)
  coin_balance        Int?      @default(0)
  loyalty_tier        String?   @default("regular")
  last_visit          DateTime?
  marketing_consent   Boolean   @default(false)
  ...
}
model CustomerBenefit { ... }
```

לכן **אין צורך לבנות מועדון מאפס** — רק לחבר את האתר ל-API שכבר קיים.

## 4. רכיבים

### 4.1 וורדפרס + WooCommerce (תשתית)

- וורדפרס עצמאי על `alenabepita.co.il` (כבר קיים, ריק ממוצרים)
- WooCommerce מותקן
- Be'teavon מסנכרן מוצרים לכאן (מנוהל על ידיהם)
- ניהול ידני של: קומבו, ספיישלים, באנרים — דרך אדמין WP

### 4.2 פלאגין `Alena-TOPALENA-Bridge` (קוד שאנחנו כותבים)

פלאגין PHP חדש שמותקן בוורדפרס. אחראי על כל החיבור ל-TOPALENA דרך REST API.

**Endpoints שיצטרכו להתווסף ב-TOPALENA:**

| Endpoint | מטרה |
|---------|------|
| `POST /api/club/lookup` | קלט: טלפון. פלט: שם לקוח, נקודות, tier, הטבה זמינה |
| `POST /api/club/orders` | אחרי הזמנה: שולח טלפון, סכום, פריטים → עדכון visit_count + coin_balance |
| `POST /api/club/register` | לקוח חדש: יוצר Customer ב-TOPALENA + מטפל ב-marketing_consent |
| `GET  /api/club/benefits/:phone` | מחזיר הטבות פעילות ללקוח |

**אבטחה:**
- JWT עם API key פרטי שיושב ב-`wp-config.php`
- כל קריאה מהפלאגין חתומה
- Endpoint ל-`lookup` מוגבל ל-rate-limit (לא חשיפת מאגר טלפונים)

**Hooks ב-WooCommerce:**
- `woocommerce_checkout_order_processed` → שליחת ההזמנה ל-TOPALENA
- `woocommerce_thankyou` → הצגת נקודות נצברות
- `woocommerce_review_order_before_payment` → תצוגת מועדון בתשלום
- `user_register` → רישום ל-TOPALENA אם סומן "הצטרף למועדון"

### 4.3 אזורי חלוקה ודמי משלוח

- WooCommerce Shipping Zones — אזור לפי מיקוד או רדיוס
- דמי משלוח לכל אזור
- מינימום הזמנה לכל אזור
- שעות פעילות משלוחים (פלאגין: **WooCommerce Business Hours** או דומה)
- **תוכן יוזן ידנית על ידי הבעלים אחרי שהאתר עולה** (לא חלק מהפיתוח)

### 4.4 פופאפים וספיישלים

- פלאגין **Popup Maker** (חינמי)
- תרחישים נתמכים:
  - פופאפ "ברוכים הבאים" עם הצעת הצטרפות למועדון
  - פופאפ יציאה עם קופון
  - באנר עליון לספיישל היום
  - מודאל "חמין יום שישי" שמופיע רק בימי חמישי-שישי
- ניהול ע"י הבעלים, ללא צורך בקוד

### 4.5 עיצוב

- תבנית מותאמת בסגנון האתר החדש של עלינא (`alenabepita.co.il` הנוכחי — צבעים, פונטים, גלריה)
- בסיס: **Astra** או **Storefront** + Customizer
- RTL מלא, נגיש בסלולר
- ביצועים: WebP, lazy-load, cache (פלאגין WP Rocket או LiteSpeed Cache)

### 4.6 התראות

- **לבעלים:** הזמנה חדשה → SMS + Email (פלאגין: WooCommerce SMS Notifications)
- **ללקוח:** אישור הזמנה, "יצאנו עם המשלוח", "המשלוח הגיע" → SMS דרך ספק קיים (Inforu/Twilio)
- אינטגרציה עם מערכת ההתראות הקיימת ב-TOPALENA (Web Push למנהלים)

## 5. תוכנית עבודה ב-4 שלבים

### שלב 0 — הכנות (יום 1)
- בעלים מתחבר לאדמין WP
- אנחנו סורקים פלאגינים, מסיר מיותרים
- מתקינים WooCommerce + תבנית בסיס
- מקשרים תעודת SSL אם חסרה

### שלב 1 — חנות בסיסית (שבוע 1)
- הקמת מטבעות, מסים, אזורי חלוקה
- חיבור PayPlus כסולק
- עיצוב ראשוני (לוגו, צבעים, דף בית)
- בדיקת סינכרון בתאבון (לאמת שמוצרים נכנסים)

### שלב 2 — חיבור TOPALENA (שבוע 2)
- הוספת 4 endpoints חדשים ל-API של TOPALENA
- בניית פלאגין `Alena-TOPALENA-Bridge` (PHP)
- בדיקות end-to-end:
  - חיפוש לקוח לפי טלפון
  - הזמנה מעלה נקודות
  - לקוח חדש נרשם
- כתיבת logs לכל קריאה (לדיבאג בעיות סינכרון)

### שלב 3 — פיצ'רים מתקדמים + השקה (שבוע 3)
- פופאפים, באנרים, ספיישלים
- התראות SMS/Email
- העברת לקוחות מ-ValueCard ל-TOPALENA (סקריפט חד-פעמי)
- השקה רכה: רק לקוחות נבחרים
- מעקב באגים שבוע נוסף

### שלב 4 (חודש אחרי) — סגירת ValueCard
- אימות שכל הלקוחות הקיימים בתוך TOPALENA
- ביטול חוזה עם ValueCard

## 6. נתונים שעדיין צריך מהבעלים

- [ ] רשימת אזורי חלוקה (שכונות / מיקודים)
- [ ] דמי משלוח לכל אזור
- [ ] מינימום הזמנה
- [ ] שעות פעילות משלוחים
- [ ] קובץ Excel/CSV של לקוחות קיימים מ-ValueCard (לסקריפט העברה)
- [ ] רשימת מבצעים/ספיישלים שכבר רצים בוולט (לטעינה ראשונית)
- [ ] גישת אדמין ל-PayPlus (אחרי האישור ב-2026-06-10)

## 7. סיכונים ומיטיגציה

| סיכון | מיטיגציה |
|------|----------|
| Be'teavon משבר סנכרון | פלאגין שלנו לא תלוי בזה — עובד גם אם בתאבון מושבת |
| TOPALENA API down בעת תשלום | הזמנה נשמרת מקומית + תור retry. הלקוח לא נחסם |
| לקוחות כפולים בין ValueCard ל-TOPALENA | סקריפט dedupe לפי טלפון לפני ההעברה |
| PayPlus לא מאושר ב-10/6 | חזרה זמנית ל-PayPal/Tranzila עד שמאושר |
| Be'teavon לא מסנכרן הכל | יכולת העלאה ידנית של מוצרים נשארת |

## 8. החלטות שדחינו (לא בסקופ הראשון)

- אפליקציה ייעודית ללקוח (יש מובייל ב-TOPALENA — מספיק)
- אינטגרציה לסליקרים נוספים מעבר ל-PayPlus
- מערכת קופונים מורכבת — נשתמש ב-WooCommerce coupons בשלב ראשון
- Headless (אופציה 3 שדחינו) — אפשר לעבור אליה בשלב 5+
