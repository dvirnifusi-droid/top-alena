import React from 'react';
import Surface from '@/components/shell/Surface';

// "צוות" — a way in, not a new screen. Everything it lists already exists.
export default function AreaTeam() {
  return <Surface surfaceKey="team" />;
}
