import React from 'react';
import Surface from '@/components/shell/Surface';

// "כסף" — a way in, not a new screen. Everything it lists already exists.
export default function AreaMoney() {
  return <Surface surfaceKey="money" />;
}
