import React from 'react';
import Surface from '@/components/shell/Surface';

// "משמרת" — a way in, not a new screen. Everything it lists already exists.
export default function AreaShift() {
  return <Surface surfaceKey="shift" />;
}
