import React from 'react';
import Surface from '@/components/shell/Surface';

// "מטבח" — a way in, not a new screen. Everything it lists already exists.
export default function AreaKitchen() {
  return <Surface surfaceKey="kitchen" />;
}
