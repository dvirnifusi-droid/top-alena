import React from 'react';
import Surface from '@/components/shell/Surface';

// "בית" — a way in, not a new screen. Everything it lists already exists.
export default function AreaHome() {
  return <Surface surfaceKey="home" />;
}
