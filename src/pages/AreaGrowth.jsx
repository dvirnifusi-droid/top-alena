import React from 'react';
import Surface from '@/components/shell/Surface';

// "צמיחה" — a way in, not a new screen. Everything it lists already exists.
export default function AreaGrowth() {
  return <Surface surfaceKey="growth" />;
}
