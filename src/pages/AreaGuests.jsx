import React from 'react';
import Surface from '@/components/shell/Surface';

// "אורחים" — a way in, not a new screen. Everything it lists already exists.
export default function AreaGuests() {
  return <Surface surfaceKey="guests" />;
}
