# Replace example.com with your real domain to get automatic HTTPS.
# For local dev, use `:80` instead.

{$DOMAIN:localhost} {
    @api path /api/*
    handle @api {
        reverse_proxy api:3001
    }
    handle {
        reverse_proxy web:80
    }
    encode gzip
}
